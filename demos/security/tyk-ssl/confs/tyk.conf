{
  "http_server_options": {
    "certificates": [
      {
        "domain_name": "gateway.tyk.local",
        "cert_file": "/etc/ssl/certs/gateway.tyk.local.cert.pem",
        "key_file": "/etc/ssl/certs/gateway.tyk.local.key.pem"
      }
    ]
  }
}
